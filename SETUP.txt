# homebrew setup

brew install libagg pkg-config fontforge

If necessary:
    brew uninstall libffi && brew install libffi

# evironment setup

conda create --name public-foundry-combinator python=3.7 pyqt numpy tensorflow=1.15
conda activate public-foundry-combinator

pip install tensor2tensor

git clone https://github.com/tensorflow/magenta.git && \
cd magenta/ && \
pip install -e .
